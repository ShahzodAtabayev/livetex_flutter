name: livetex_flutter
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  web_socket_channel: ^3.0.1
  rxdart: ^0.28.0
  http: ^1.2.2
  connectivity_plus: ^6.0.5
  either_dart: ^1.0.0
  dio: ^5.7.0
  path: ^1.9.0
  uuid: ^4.5.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  plugin:
    platforms:
      android:
        package: com.shahzod.livetex_flutter
        pluginClass: LivetexFlutterPlugin
      ios:
        pluginClass: LivetexFlutterPlugin
